/**
 * MEssage bus - publish subscribe unsubscribe
 * subscription - messagetype callback
 */

class Subscription {}

class MessageBus {
  constructor() {}

  publish(sourceId, messageType, message) {}

  subscribe(sourceId, messageType, callback) {}

  unsubscribe(subscriptionId) {}
}
